下图中，（a）是结点结构，llink指向结点的前驱，rlink指向结点的后继，指针s指向的待插入结点（如b所示），双向链表片段如c所示，写出在p指针指向的结点前面插入指针s指向的结点的操作片段。
（只能使用指针变量p和s，不得定义新的指针变量，图c中a,b,c是数据，不是结点的指针）

s-＞rlink=p；

s-＞liink=p-＞llink；

p-＞llink-＞rlink=s；

p-＞llink=s；